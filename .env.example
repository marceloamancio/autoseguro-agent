# Copie para .env e preencha com a SUA chave. O .env real fica fora do git.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx      # sua chave Anthropic
ANTHROPIC_MODEL=claude-sonnet-5        # modelo do agente (default)
QUOTE_API_URL=http://localhost:8000    # aponta para o quote-service do desafio
